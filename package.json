{
  "name": "parkpass-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace=@parkpass/api run dev",
    "dev:web": "npm --workspace=@parkpass/web run dev",
    "build": "npm --workspace=@parkpass/shared run build && npm --workspace=@parkpass/api run build && npm --workspace=@parkpass/web run build",
    "build:api": "npx prisma generate && npm --workspace=@parkpass/shared run build && npm --workspace=@parkpass/api run build",
    "start:api": "npm --workspace=@parkpass/api run start",
    "db:push": "prisma db push",
    "db:seed": "node prisma/seed.js",
    "test": "npm --workspace=@parkpass/api run test"
  },
  "devDependencies": {
    "prisma": "^6.4.0"
  },
  "dependencies": {
    "@prisma/client": "^6.4.0",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3"
  }
}
